# Assignment 4 Buddy Task 1

Algorithms:
- K-Means Clustering
- FastMap

Build the project with C++17.

Generate test files by compiling and running generator.cpp.

The driver supports running one test or all tests for the selected algorithm.

The K-Means and FastMap algorithm implementations are placeholders and still need to be implemented in src/kmeans.cpp and src/fastmap.cpp.
